{
  "mcpServers": {
    "codegraph": {
      "type": "stdio",
      "command": "/data/dazheng/.local/bin/codegraph",
      "args": [
        "serve",
        "--mcp",
        "--path",
        "${workspaceFolder}"
      ]
    }
  }
}
